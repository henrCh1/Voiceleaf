pluginManagement {
    // Plugins resolve from the official portals (reliable for the small plugin-marker artifacts).
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    // Libraries (the bulk of the download) come from domestic mirrors first, official as fallback.
    repositories {
        maven(url = "https://maven.aliyun.com/repository/google")
        maven(url = "https://maven.aliyun.com/repository/public")
        google()
        mavenCentral()
    }
}

rootProject.name = "V2J"
include(":app")
